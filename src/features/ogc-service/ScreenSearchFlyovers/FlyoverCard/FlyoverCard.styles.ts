import { createStyles } from '@mantine/core';

export default createStyles((theme) => ({
    card: {
        backgroundColor: theme.white,
    },
    title: {
        fontWeight: 500,
        lineHeight: 1,
    },
    date: {
        fontWeight: 400,
        lineHeight: 1,
    }
}));